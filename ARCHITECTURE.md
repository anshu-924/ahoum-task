# 🎯 Application Architecture & Flow

## System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                         User Browser                         │
│                     http://localhost                         │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                    Nginx Reverse Proxy                       │
│                        (Port 80)                             │
│  ┌────────────────┬────────────────┬─────────────────────┐  │
│  │ / → Frontend   │ /api → Backend │ /admin → Backend    │  │
│  └────────┬───────┴────────┬───────┴──────────┬──────────┘  │
└───────────┼────────────────┼──────────────────┼─────────────┘
            │                │                  │
            ▼                ▼                  ▼
┌─────────────────┐  ┌──────────────────┐  ┌──────────────┐
│  React Frontend │  │ Django Backend   │  │ Django Admin │
│   (Port 3000)   │  │   (Port 8000)    │  │              │
│                 │  │                  │  │              │
│  - Home         │  │  - REST API      │  │  - User Mgmt │
│  - Login        │  │  - Auth/JWT      │  │  - Sessions  │
│  - Dashboards   │  │  - Sessions      │  │  - Bookings  │
│  - Sessions     │  │  - Bookings      │  │              │
│  - Profile      │  │  - Payments      │  │              │
└─────────────────┘  └────────┬─────────┘  └──────────────┘
                              │
                              ▼
                    ┌──────────────────┐
                    │   PostgreSQL     │
                    │   (Port 5432)    │
                    │                  │
                    │  - Users         │
                    │  - Sessions      │
                    │  - Bookings      │
                    └──────────────────┘
```

## User Flow Diagrams

### Student Journey

```
┌─────────┐
│  Start  │
└────┬────┘
     │
     ▼
┌─────────────────┐
│   Home Page     │ ← Browse Sessions
│   (Public)      │   View Categories
└────┬────────────┘   Filter Sessions
     │
     ├─────────────────────────────┐
     │                             │
     ▼                             ▼
┌─────────────┐           ┌────────────────┐
│ View Session│           │   Need Login   │
│   Details   │           │   to Book      │
└────┬────────┘           └────┬───────────┘
     │                         │
     │                         ▼
     │                  ┌──────────────┐
     │                  │  Login Page  │
     │                  │ Select: User │
     │                  │  Demo Login  │
     │                  └────┬─────────┘
     │                       │
     ▼                       ▼
┌──────────────────────────────────┐
│      Book Session Form           │
│  - Date/Time                     │
│  - Attendee Count                │
│  - Notes                         │
└────┬─────────────────────────────┘
     │
     ▼
┌──────────────────────┐
│   User Dashboard     │
│  - View Bookings     │
│  - Check Status      │
│  - Manage Profile    │
└──────────────────────┘
```

### Tutor/Creator Journey

```
┌─────────┐
│  Start  │
└────┬────┘
     │
     ▼
┌─────────────────┐
│   Login Page    │
│ Select: Creator │
│   Demo Login    │
└────┬────────────┘
     │
     ▼
┌─────────────────────────┐
│  Creator Dashboard      │
│  - View Stats           │
│  - Total Sessions       │
│  - Total Bookings       │
│  - Revenue              │
└────┬────────────────────┘
     │
     ├───────────────────────┬────────────────────┐
     │                       │                    │
     ▼                       ▼                    ▼
┌────────────┐    ┌──────────────────┐   ┌─────────────┐
│   Create   │    │  Manage Sessions │   │   Manage    │
│  Session   │    │  - Edit          │   │  Bookings   │
│   Form     │    │  - View Stats    │   │  - Confirm  │
└────────────┘    └──────────────────┘   │  - Cancel   │
                                          └─────────────┘
```

## Data Flow

### Authentication Flow

```
┌──────────┐         ┌───────────┐         ┌──────────┐
│ Frontend │────1───▶│  Backend  │────2───▶│   OAuth  │
│          │         │           │         │ Provider │
│          │◀───4────│           │◀───3────│          │
└──────────┘         └───────────┘         └──────────┘
     │
     │ 5. Store JWT
     ▼
┌────────────┐
│localStorage│
│ - access   │
│ - refresh  │
└────────────┘

1. User clicks "Login with Google"
2. Backend redirects to OAuth provider
3. User authorizes, provider returns token
4. Backend validates & issues JWT
5. Frontend stores JWT in localStorage
```

### Booking Flow

```
┌─────────┐                    ┌──────────┐
│ Student │                    │  Creator │
└────┬────┘                    └────┬─────┘
     │                              │
     │ 1. Browse Sessions           │
     ├────────────────────────┐     │
     │                        │     │
     │ 2. Select Session      │     │
     │                        │     │
     │ 3. Fill Booking Form   │     │
     │    - Date              │     │
     │    - Attendees         │     │
     │    - Notes             │     │
     │                        │     │
     │ 4. Submit Booking      │     │
     │         │              │     │
     │         ▼              │     │
     │   ┌──────────┐         │     │
     │   │ Backend  │         │     │
     │   │ Creates  │         │     │
     │   │ Booking  │         │     │
     │   └────┬─────┘         │     │
     │        │               │     │
     │        ▼               │     │
     │  Status: Pending ──────┼────▶│
     │                        │     │
     │                        │  5. Creator
     │                        │     views booking
     │                        │     │
     │                        │  6. Confirms
     │◀───────────────────────┼─────┤
     │   Notification         │     │
     │   Status: Confirmed    │     │
     │                        │     │
```

## Component Hierarchy

```
App
├── AuthProvider (Context)
│   └── Router
│       ├── Navbar
│       │   ├── Logo
│       │   ├── NavLinks
│       │   └── AuthButtons
│       │
│       └── Routes
│           ├── Home
│           │   ├── Hero
│           │   ├── Filters
│           │   └── SessionCard[]
│           │
│           ├── Login
│           │   ├── RoleSelector
│           │   ├── OAuthButtons
│           │   └── DemoLogin
│           │
│           ├── SessionDetail
│           │   ├── SessionHeader
│           │   ├── SessionDescription
│           │   └── BookingForm
│           │
│           ├── UserDashboard
│           │   ├── StatsCards
│           │   ├── BookingCard[]
│           │   └── ProfileCard
│           │
│           ├── CreatorDashboard
│           │   ├── StatsCards
│           │   ├── TabNavigation
│           │   ├── SessionCard[]
│           │   └── BookingCard[]
│           │
│           ├── Profile
│           │   └── ProfileForm
│           │
│           └── CreateSession
│               └── SessionForm
```

## State Management

### Auth Context State

```javascript
{
  user: {
    id: 1,
    email: "user@example.com",
    first_name: "John",
    last_name: "Doe",
    role: "user" | "creator",
    bio: "...",
    phone: "..."
  },
  isAuthenticated: true,
  isCreator: false,
  loading: false
}
```

### Local Storage

```
localStorage
├── access_token: "eyJ0eXAiOiJKV1QiLCJh..."
└── refresh_token: "eyJ0eXAiOiJKV1QiLCJh..."
```

## API Request Flow

```
Component
    │
    │ 1. Call API function
    ▼
api.js (Axios)
    │
    │ 2. Add JWT token (interceptor)
    ▼
Request to Backend
    │
    │ 3. Backend processes
    ▼
Response
    │
    │ 4. Check 401? Auto refresh token
    ▼
Component receives data
    │
    │ 5. Update UI
    ▼
User sees result
```

## Docker Container Interaction

```
┌─────────────────────────────────────────────────────────┐
│                   Docker Network                        │
│                  (sessions_network)                     │
│                                                         │
│  ┌──────────┐    ┌──────────┐    ┌──────────┐        │
│  │    DB    │◀───│ Backend  │◀───│  Nginx   │◀───┐   │
│  │          │    │          │    │          │    │   │
│  │ Port     │    │ Port     │    │ Port     │    │   │
│  │ 5432     │    │ 8000     │    │ 80       │    │   │
│  └──────────┘    └──────────┘    └─────▲────┘    │   │
│                                         │         │   │
│                  ┌──────────┐           │         │   │
│                  │ Frontend │───────────┘         │   │
│                  │          │                     │   │
│                  │ Port     │                     │   │
│                  │ 3000     │                     │   │
│                  └──────────┘                     │   │
│                                                   │   │
└───────────────────────────────────────────────────┼───┘
                                                    │
                                            ┌───────▼────┐
                                            │   User     │
                                            │  Browser   │
                                            └────────────┘
```

## Deployment Sequence

```
1. docker-compose up --build
        │
        ├─ Build Backend Image
        │  └─ Install Python deps
        │     └─ Copy Django code
        │
        ├─ Build Frontend Image
        │  └─ Install npm packages
        │     └─ Build React app
        │        └─ Copy to Nginx
        │
        ├─ Start PostgreSQL
        │  └─ Initialize database
        │
        ├─ Start Backend
        │  └─ Wait for DB
        │     └─ Run migrations
        │        └─ Start Gunicorn
        │
        ├─ Start Frontend
        │  └─ Start Nginx (serves React)
        │
        └─ Start Nginx (reverse proxy)
           └─ Route requests
              ├─ / → Frontend
              ├─ /api → Backend
              └─ /admin → Backend

2. Application Ready! ✅
```

## Security Flow

```
┌──────────┐
│  Request │
└────┬─────┘
     │
     ▼
┌─────────────────┐
│ Nginx (Port 80) │
│  - SSL Ready    │
│  - Rate Limit   │
└────┬────────────┘
     │
     ▼
┌─────────────────────┐
│ Backend Middleware  │
│  - CORS Check       │
│  - JWT Validation   │
│  - Permission Check │
└────┬────────────────┘
     │
     ▼
┌─────────────┐
│   API View  │
│  - Process  │
│  - Validate │
└────┬────────┘
     │
     ▼
┌──────────────┐
│  Database    │
│  - Query     │
│  - Sanitize  │
└──────────────┘
```

## Summary

This architecture provides:

✅ **Separation of Concerns** - Frontend, Backend, Database
✅ **Scalability** - Each service can scale independently
✅ **Security** - JWT auth, CORS, permissions
✅ **Maintainability** - Clear structure, documented
✅ **Deployment** - One command to run everything
✅ **Development** - Easy to develop locally
✅ **Production Ready** - With minor config changes
